package rcs;

import static rcs.utilities.getConfig;

public class info {

    public static String prefix = getConfig().getPrefix();
    public static String token = getConfig().getToken();
    public static long id = getConfig().getId();
    
}
